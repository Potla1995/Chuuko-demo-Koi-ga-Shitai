language: minimal

script:
    - cd 3/
    - wget https://github.com/rust-lang/mdBook/releases/download/v0.3.0/mdbook-v0.3.0-x86_64-unknown-linux-gnu.tar.gz
    - tar -xzf mdbook-v0.3.0-x86_64-unknown-linux-gnu.tar.gz
    - ./mdbook build
    - rm -rf mdbook*
    - git add .

deploy:
    provider: pages
    cleanup: false
    github_token: $GIT_TOKEN
    keep_history: false
    committer_from_gh: true
    on:
        branch: master
